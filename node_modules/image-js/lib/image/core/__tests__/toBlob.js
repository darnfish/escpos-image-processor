"use strict";

var _common = require("test/common");

var _canvas = require("canvas");

describe.skip('Image core toBlob', function () {
  it('constructor defaults', function () {
    let canvas = (0, _canvas.createCanvas)(2, 2);
    let ctx = canvas.getContext('2d');
    ctx.fillStyle = 'red';
    ctx.fillRect(0, 0, 2, 1);

    let img = _common.Image.fromCanvas(canvas);

    return img.toBlob().then(function () {// empty
    });
  });
});