"use strict";

var _common = require("test/common");

it('should return a number', function () {
  let image = new _common.Image(1, 2, [230, 100], {
    kind: 'GREY'
  });
  const threshold = image.getThreshold();
  expect(typeof threshold).toBe('number');
});