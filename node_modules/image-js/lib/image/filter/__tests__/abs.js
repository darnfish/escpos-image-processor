"use strict";

var _common = require("test/common");

test('abs fiter', function () {
  const img = new _common.Image(1, 2, [-2000, 3000], {
    kind: 'GREY',
    bitDepth: 32
  });
  const out = img.abs();
  expect(Array.from(out.data)).toEqual([2000, 3000]);
});